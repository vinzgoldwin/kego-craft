# KEGO portfolio mobile v2

30 seconds · 1080×1920 · 30 fps · HyperFrames 0.8.40.

Preserves the previous preferred intro, hero and projects footage. Adds a dedicated six-second Experience chapter using all five milestones, exact source titles and roles, and original artwork from `app/experience-timeline.tsx`. Body paragraphs are intentionally omitted in this video-only layout for readability. Services and Contact retain the previous style.

The original `videos/portfolio-mobile` and application source are unchanged.

## Run

```sh
npm install
npm run dev
npm run check
npm run render
```

Output: `renders/kego-portfolio-mobile-v2.mp4`. The local project retains its existing website font. No narration; original procedural instrumental soundtrack.
